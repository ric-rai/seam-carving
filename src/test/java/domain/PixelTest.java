package domain;

/*
public class PixelTest {
    Pixel testPixel;

    @Before
    public void setTestPixel() {
        testPixel = new Pixel();
        testPixel.red = 1;
        testPixel.green = 127;
        testPixel.blue = 255;
    }

    @Test
    public void constructorsWorkCorrectly() {
        int red = 1, green = 127, blue = 255;
        Pixel pixel = new Pixel(1, 1, red, green, blue);
        afterConstructionCheck(pixel);
        pixel = new Pixel(1, 1, getArgbIntFromRgbValues(red, green, blue));
        afterConstructionCheck(pixel);
        byte bRed = 1, bGreen = 127, bBlue = -1;
        pixel = new Pixel(1, 1, bRed, bGreen, bBlue);
        afterConstructionCheck(pixel);
    }

    private void afterConstructionCheck(Pixel pixel) {
        assertThat(pixel.red, is(1));
        assertThat(pixel.green, is(127));
        assertThat(pixel.blue, is(255));
        assertThat(pixel.row, is(1));
        assertThat(pixel.col, is(1));
    }

    @Test
    public void equalsWorksCorrectly() {
        assertThat(testPixel.equals(new Object()), is(false));
        assertThat(testPixel.equals(new Pixel(1, 1, 1, 127, 255)), is(true));
        assertThat(testPixel.equals(new Pixel(1, 1, 0, 0, 0)), is(false));
    }

}
*/
